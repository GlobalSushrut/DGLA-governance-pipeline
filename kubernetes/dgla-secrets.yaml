apiVersion: v1
kind: Secret
metadata:
  name: dgla-secrets
  namespace: dgla
type: Opaque
stringData:
  # These values should be replaced in a production environment
  # Example: Use a CI/CD pipeline with environment variables or a secrets manager
  jwt-secret: REPLACE_WITH_SECURE_JWT_SECRET
  redis-password: REPLACE_WITH_SECURE_REDIS_PASSWORD
